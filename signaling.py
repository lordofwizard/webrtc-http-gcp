# This is the http signaling server that will send request

# Some routes
# /robot -> sends 
# /video_device -> sends it's own sdp and grabs the client sdp.
# /robot_check -> checks every one second for response of client sdp. 

